services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000}
    ports:
      - "5173:80"

  test:
    build:
      context: .
      dockerfile: Dockerfile
      target: test
    profiles:
      - test
